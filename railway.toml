[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npx prisma db push --skip-generate && npx next start -p ${PORT:-8080}"
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
